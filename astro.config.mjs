// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Canonical host — also used to build absolute og:image URLs.
  site: 'https://lyonsshare.mikelyons.org',
});
